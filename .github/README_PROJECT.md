To create a GitHub repo and push this project from your machine:
1. git init
2. git add .
3. git commit -m "Initial commit - GreenClicker Final NDK"
4. gh repo create <owner>/<repo> --public --source=. --remote=origin
5. git push -u origin main
