// Example snippet: convert downscaled bitmap to RGBA and call NativeProcessor.findGreen(...)
// val rgba = NativeProcessor.bitmapToRGBA(smallBitmap)
// val res = NativeProcessor.findGreen(rgba, smallBitmap.width, smallBitmap.height, step, hMin, hMax, sMin, vMin)
// if (res[0] >= 0) { val mappedX = res[0] * scale; val mappedY = res[1] * scale; ... }
